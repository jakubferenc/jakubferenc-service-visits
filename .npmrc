engine-strict=true

# allow native builds (required for better-sqlite3)
ignore-scripts=false

# ensure pnpm does not isolate builds for native modules
shamefully-hoist=true

# pnpm workspace settings (optional but recommended)
auto-install-peers=true
strict-peer-dependencies=false
